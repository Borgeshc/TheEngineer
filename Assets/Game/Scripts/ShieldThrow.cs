using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShieldThrow : MonoBehaviour 
{/*
	public KeyCode keyCode;
	public GameObject shieldObjectParent;
	public GameObject shieldObject;
	public GameObject shieldChild;

	ShieldThrowMovement shieldThrowMovement;

	// -40 90 -130
	Transform originalPosition;

	void Start()
	{
		originalPosition = shieldChild.transform;
		shieldThrowMovement = shieldObject.GetComponent<ShieldThrowMovement> ();
	}

	void Update () 
	{
		if (Input.GetKeyDown (keyCode) && TargetObject.target != null) 
		{
			shieldObject.transform.SetParent (null);
			shieldChild.transform.rotation = new Quaternion (-40, 90, -130,0);
			shieldThrowMovement.enabled = true;
			shieldThrowMovement.SetTarget (TargetObject.target, originalPosition.gameObject);
		}
	}*/
}
